#include <iostream>
#include <bits/stdc++.h> // this containns all the libraries in this headerfile.

using namespace std;


int main ()
{
    int a;
    cin >> a; // this is work becoz of this (using namespace std;) without this we have to write something extra code..
     

    // //without with (using namespace std;) we have to write this code 
    // std :: cin >> a;
    // std cout << a;
    
    return 0;
}